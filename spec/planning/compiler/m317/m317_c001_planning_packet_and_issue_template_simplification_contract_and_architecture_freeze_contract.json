{
  "mode": "m317-c001-planning-packet-issue-template-contract-v1",
  "contract_id": "objc3c-cleanup-planning-packet-issue-template-contract/m317-c001-v1",
  "issue_body_contract": {
    "required_sections": [
      "## Outcome",
      "## Why this matters",
      "## Acceptance criteria",
      "## Primary implementation surfaces",
      "## Dependencies",
      "## Validation posture",
      "<!-- EXECUTION-ORDER-START -->"
    ],
    "optional_sections": [
      "## Design corrections folded in",
      "## Notes",
      "## Boundary note",
      "## Post-cleanup dependency rewrite"
    ],
    "default_rules": [
      "Do not include a universal Required deliverables section by default.",
      "Do not duplicate milestone-level prose mechanically into every issue body.",
      "Use optional sections only when they remove ambiguity or preserve an already-required boundary."
    ]
  },
  "planning_packet_contract": {
    "required_sections": [
      "## Intent",
      "## Scope",
      "## Contract",
      "## Validation plan",
      "## Closeout evidence",
      "## Next issue"
    ],
    "optional_sections": [
      "## Why this issue exists",
      "## Migration notes",
      "## Implementation notes"
    ]
  },
  "validation_postures": [
    {
      "code": "shared_acceptance_harness",
      "default_validation": [
        "shared harness or executable integration proof"
      ]
    },
    {
      "code": "static_policy_guard",
      "default_validation": [
        "targeted static checker",
        "pytest only if the guard has nontrivial parsing or policy logic"
      ]
    },
    {
      "code": "migration_bridge",
      "default_validation": [
        "migration verifier",
        "compatibility evidence"
      ]
    },
    {
      "code": "generator_contract_only",
      "default_validation": [
        "template or schema checker",
        "no readiness runner by default"
      ]
    }
  ],
  "generator_input_fields": [
    "issue_code",
    "title_core",
    "kind",
    "milestone_focus_summary",
    "acceptance_criteria",
    "primary_implementation_surfaces",
    "dependencies",
    "validation_posture",
    "optional_sections"
  ],
  "prohibited_defaults": [
    "Assuming every issue gets a dedicated checker, readiness runner, and pytest bundle.",
    "Auto-inserting Required deliverables boilerplate into every issue body.",
    "Generating temporary milestone-specific execution-body artifacts outside tmp/.",
    "Duplicating milestone focus/exit-gate prose mechanically into issue-local packets beyond what the issue needs."
  ],
  "future_implementation_surfaces": [
    ".github/ISSUE_TEMPLATE/roadmap_execution.yml",
    ".github/ISSUE_TEMPLATE/conformance_execution.yml",
    "tmp/planning/cleanup_acceleration_program/generate_cleanup_acceleration_program.py",
    "tmp/planning/post_m292_refined_program/post_m292_refined_program_seed.json"
  ],
  "next_issue": "M317-C002"
}
