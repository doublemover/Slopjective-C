# M313-D001 Planning Packet

## Goal
- Freeze one explicit CI validation topology and scheduling order for the `M313` cleanup program.

## Scope
- Freeze the retained static-guard set.
- Freeze the acceptance-first stage order and canonical report roots.
- Freeze the dedicated workflow path and runner stages that `M313-D002` must implement.

## Focus
- CI topology and scheduling order over shared acceptance suites and compatibility bridges.
- Canonical `tmp/reports/m313/ci/**` layout.
- Dedicated acceptance-first workflow path instead of more ad hoc checklist chains.

## Next issue
- Next issue: `M313-D002`.
