# M321 Error Runtime Closure Planning Root

This directory holds milestone-local planning helpers and replay notes for `M321`.

Canonical checked-in contracts live under:

- `tests/tooling/fixtures/error_runtime_closure/`

Canonical generated evidence lives under:

- `tmp/reports/m321/`

The milestone is intentionally bound to the shared runtime acceptance, runnable error conformance, and runnable error end-to-end flows instead of introducing a parallel execution path.
