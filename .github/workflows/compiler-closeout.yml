name: Compiler Closeout (Legacy Manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-closeout-note:
    runs-on: ubuntu-latest
    steps:
      - name: Explain legacy workflow status
        run: |
          echo "This legacy roadmap closeout workflow is manual-only."
          echo "Active validation now lives in ObjC3C Core CI, Spec Quality, and Publish Spec."
