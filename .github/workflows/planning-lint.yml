name: Planning Lint (Legacy Manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-planning-note:
    runs-on: ubuntu-latest
    steps:
      - name: Explain legacy workflow status
        run: |
          echo "This legacy local-planning workflow is manual-only."
          echo "Planning source of truth now lives primarily in GitHub milestones and issues."
