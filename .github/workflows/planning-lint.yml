name: Planning Lint (Manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  planning-source-note:
    runs-on: ubuntu-latest
    steps:
      - name: Explain planning source status
        run: |
          echo "This local-planning workflow is manual-only."
          echo "Planning source of truth now lives primarily in GitHub milestones and issues."
