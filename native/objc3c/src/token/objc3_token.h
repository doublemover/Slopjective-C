#pragma once

#include "token/objc3_token_contract.h"

// Backward-compatible aliases while callers migrate to explicit contract names.
using TokenKind = Objc3LexTokenKind;
using Token = Objc3LexToken;
