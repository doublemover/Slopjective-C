{
  "id": "EC-CROSS-04",
  "title": "Accept try? await composition with optional chaining and postfix propagation",
  "feature": "try await + postfix ? + ?.",
  "bucket": "semantic",
  "profile": "strict",
  "references": [
    "docs/reference/legacy_spec_anchor_index.md#f-5-6",
    "docs/reference/legacy_spec_anchor_index.md#am-6"
  ],
  "source": "id _Nullable nextTokenOrNil(Client * _Nullable client) async {\n    return (try? await client?.fetchToken())?;\n}\n",
  "expect": {
    "parse": "accept",
    "diagnostics": []
  }
}
