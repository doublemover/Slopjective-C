{
  "id": "TYP-8013-01",
  "title": "Accept typed object, nullability, protocol composition, generic suffix, optional, and key-path semantic closure",
  "feature": "Objective-C 3.0 typed semantic model closure",
  "bucket": "semantic",
  "profile": "strict",
  "references": [
    "tests/tooling/fixtures/native/type_semantic_model_closure_positive.objc3",
    "tmp/reports/claimability/type-semantic-model-closure/type_semantic_model_closure_summary.json"
  ],
  "source": "module typeSemanticModelClosure;\n\n@protocol SemanticValue\n@required\n@property (readonly) id requiredToken;\n- (id<SemanticValue>*?)requiredNode;\n@optional\n@property (readonly) id optionalToken;\n@end\n\n@interface SemanticBox\n@property (readonly) id title;\n@end\n\n@implementation SemanticBox\n@end\n\nfn choose(flag: bool, when_true: id<SemanticValue>*?, when_false: id<SemanticValue>*!) -> id<SemanticValue>*? {\n  if (flag) {\n    return when_true;\n  }\n  return when_false;\n}\n\nfn consumeBox(box: SemanticBox *?) -> id? {\n  return [box? title];\n}\n\nfn main(maybeValue: id?, retiredRoute: id, box: id<SemanticValue>*?) -> i32 {\n  if let chosen = maybeValue {\n    let local = chosen;\n  } else {\n    return 1;\n  }\n\n  guard let ready = maybeValue else {\n    return 2;\n  }\n\n  let resolved = maybeValue ?? retiredRoute;\n  let sent = [maybeValue? description];\n  let path = @keypath(SemanticBox, title);\n  let selected = choose(true, box, retiredRoute);\n  return 0;\n}\n",
  "expect": {
    "parse": "accept",
    "diagnostics": [],
    "manifest_surface": "frontend.pipeline.semantic_surface.objc_type_system_type_semantic_model"
  }
}
