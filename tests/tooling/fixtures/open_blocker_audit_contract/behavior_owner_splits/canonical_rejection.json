{
  "schema_version": 1,
  "catalog": "objc3c-open-blocker-audit-contract-behavior-owner-split",
  "owner_phase": "canonical_rejection",
  "source_contracts": [
    "tests/tooling/fixtures/open_blocker_audit_contract/hard_fail_*"
  ],
  "support_boundary": "canonical rejection owns open blocker audit contract rows that look like support but are hard-fail drift, old-mode, gate, retired route, compatibility, generated-only, evidence-log, migration-only, wrapper-only, runner-only, workflow-label-only, or directory-label-only. Contract drift rows are non-positive even when they include summary JSON or snapshot provenance.",
  "phase_support_claim_authority": false,
  "negative_claim_authority": true,
  "selectors": [
    "tests/tooling/fixtures/open_blocker_audit_contract/hard_fail_*",
    "drift_class:log_contract_drift",
    "drift_class:snapshot_parity_key_order_drift",
    "drift_class:status_exit_mismatch",
    "drift_class:summary_schema_drift",
    "*old-mode*",
    "*old_mode*",
    "*gate*",
    "*retired-route*",
    "*compatibility*",
    "*compat*",
    "*migration-only*",
    "*migration_only*",
    "*evidence-log*",
    "*evidence_log*",
    "*generated-only*",
    "*generated_only*",
    "*wrapper-only*",
    "*wrapper_only*",
    "*runner-only*",
    "*runner_only*"
  ],
  "canonical_anchor": "tests/conformance/hard_cutover_retired_surface_fixture_contracts.json",
  "retired_surface_rule": "hard-fail drift rows, old-mode audit, gate audit, retired route audit, compatibility audit, migration-only audit, evidence-log audit, generated-only audit, wrapper-only audit, runner-only audit, and alternate acceptance audit claims are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
