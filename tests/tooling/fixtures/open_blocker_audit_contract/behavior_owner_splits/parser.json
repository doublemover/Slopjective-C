{
  "schema_version": 1,
  "catalog": "objc3c-open-blocker-audit-contract-behavior-owner-split",
  "owner_phase": "parser",
  "source_contracts": [
    "tests/tooling/fixtures/open_blocker_audit_contract"
  ],
  "support_boundary": "parser ownership is non-positive for open blocker audit contract fixtures because this family contains tooling summaries, snapshots, logs, and expected status files, not hand-authored Objective-C 3 parser fixtures. Parser behavior claims must point back to canonical parser fixture owners.",
  "phase_support_claim_authority": false,
  "selectors": [
    "absent:tests/tooling/fixtures/open_blocker_audit_contract/**/*.objc3",
    "tooling_fixture_family:open_blocker_audit_contract",
    "compiler_phase_boundary:no-parser-positive-truth",
    "upstream_owner_required:tests/native/parser"
  ],
  "canonical_anchor": "tests/native/parser",
  "retired_surface_rule": "old-mode parser audit, shim parser audit, fallback parser audit, compatibility parser audit, generated-only parser rows, report-only parser rows, migration-only parser rows, and parser claims derived only from audit snapshots are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
