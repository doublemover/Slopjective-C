# Fixture Public Claims

The fixture runtime surface is supported by `scripts/good_evidence.py` and the
`fixture-public-evidence` family.

## Unsupported Boundary

Non-goal: universal foreign topology support remains unsupported and fail-closed
until a checked-in runtime ABI contract and executable evidence exist.
