{
  "schema_version": 1,
  "catalog": "objc3c-release-operations-behavior-owner-split",
  "owner_phase": "runtime",
  "source_contracts": [
    "tests/tooling/fixtures/release_operations/fail_closed_diagnostics_policy.json",
    "tests/tooling/fixtures/release_operations/update_channel_policy.json",
    "tests/tooling/fixtures/release_operations/upgrade_path_surface.json"
  ],
  "support_boundary": "runtime ownership is non-positive for this release-operations family except for provenance routing to canonical runtime owners. Runtime-metadata mismatch diagnostics and revert primitive labels are release-operation fail-closed policy labels here; runtime execution behavior remains owned by canonical runtime fixtures.",
  "phase_support_claim_authority": false,
  "selectors": [
    "diagnostic_id:runtime-metadata-contract-mismatch",
    "forbidden_behavior:runtime alternate-path publication",
    "revert_primitive:local-installer",
    "revert_primitive:offline-bundle",
    "compiler_phase_boundary:no-runtime-positive-truth-from-release-operations-contracts",
    "upstream_owner_required:tests/native/runtime"
  ],
  "canonical_anchor": "tests/native/runtime",
  "retired_surface_rule": "old-mode runtime release-operation rows, shim runtime release-operation rows, fallback runtime release-operation rows, compatibility runtime release-operation rows, migration-only runtime rows, generated-only runtime rows, report-only runtime rows, runtime-diagnostic-label-only support, alternate runtime publication paths, and runner-only success are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
