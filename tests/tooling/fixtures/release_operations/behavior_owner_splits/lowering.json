{
  "schema_version": 1,
  "catalog": "objc3c-release-operations-behavior-owner-split",
  "owner_phase": "lowering",
  "source_contracts": [
    "tests/tooling/fixtures/release_operations"
  ],
  "support_boundary": "lowering ownership is non-positive for this release-operations family. The family contains JSON release-operation policy fixtures only, with no checked-in lowering output truth.",
  "phase_support_claim_authority": false,
  "selectors": [
    "absent:tests/tooling/fixtures/release_operations/**/*-lowering.expect.txt",
    "tooling_fixture_family:release_operations",
    "compiler_phase_boundary:no-lowering-positive-truth-from-release-operations-contracts",
    "upstream_owner_required:tests/native/lowering"
  ],
  "canonical_anchor": "tests/native/lowering",
  "retired_surface_rule": "old-mode lowering release-operation rows, gate lowering release-operation rows, retired route lowering release-operation rows, compatibility lowering release-operation rows, migration-only lowering rows, generated-only lowering rows, evidence-log lowering rows, and release-policy-only lowering support are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
