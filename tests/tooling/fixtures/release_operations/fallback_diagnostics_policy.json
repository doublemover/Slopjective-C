{
  "contract_id": "objc3c.release.operations.fail_closed.diagnostics.policy.v1",
  "schema_version": 1,
  "diagnostic_classes": [
    {
      "diagnostic_id": "abi-major-mismatch",
      "severity": "error",
      "trigger": "requested update crosses major ABI line",
      "required_action": "block upgrade and require same-major rollback channel"
    },
    {
      "diagnostic_id": "runtime-metadata-contract-mismatch",
      "severity": "error",
      "trigger": "candidate or preview channel requires retired runtime metadata behavior",
      "required_action": "block publication and require a same-major rollback channel"
    },
    {
      "diagnostic_id": "data-format-migration-required",
      "severity": "error",
      "trigger": "offline bundle or installer receipt format changes across minor lines",
      "required_action": "block publication until generated migration and rollback evidence exists"
    },
    {
      "diagnostic_id": "deprecated-channel-selected",
      "severity": "warn",
      "trigger": "requested channel is outside its support window",
      "required_action": "publish deprecated-channel warning and recommend stable rollback"
    }
  ],
  "rollback_primitives": [
    "local-installer",
    "offline-bundle"
  ],
  "forbidden_behaviors": [
    "silent cross-major upgrade",
    "warning-free preview promotion",
    "rollback path omission",
    "runtime fallback publication",
    "data-format fallback publication"
  ]
}
