{
  "schema_version": 1,
  "catalog": "objc3c-performance-behavior-owner-split",
  "owner_phase": "ir",
  "benchmark_portfolio": "tests/tooling/fixtures/performance/benchmark_portfolio.json",
  "benchmark_parameters": "tests/tooling/fixtures/performance/benchmark_parameters.json",
  "support_boundary": "IR ownership covers artifact, package, telemetry, and compile-output provenance only when canonical IR owners claim the behavior; generated reports, package staging, report roots, artifact roots, output roots, raw samples, normalized values, cache rows, comparative baselines, machine profiles, tool versions, and public command wrappers never define IR support",
  "phase_support_claim_authority": false,
  "selectors": [
    "artifact_root:tmp/artifacts/performance",
    "package_stage_root:tmp/pkg/objc3c-native-runnable-toolchain",
    "telemetry_schema:schemas/objc3c-performance-telemetry-v1.schema.json",
    "compile_latency:output_root",
    "runtime_wall_clock:output_root"
  ],
  "canonical_anchor": "tests/native/ir",
  "retired_surface_rule": "generated artifacts, package staging, telemetry packets, normalized report values, generated-only IR rows, evidence-log IR rows, migration-only IR rows, timing-only IR rows, cache-only IR rows, wrapper-only IR rows, local-only IR rows, cross-machine IR rows, foreign-baseline IR rows, shim/fallback artifact paths, old-mode IR rows, and compatibility outputs cannot convert into canonical IR support"
}
