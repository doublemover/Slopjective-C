{
  "schema_version": 1,
  "catalog": "objc3c-performance-behavior-owner-split",
  "owner_phase": "lowering",
  "benchmark_portfolio": "tests/tooling/fixtures/performance/benchmark_portfolio.json",
  "benchmark_parameters": "tests/tooling/fixtures/performance/benchmark_parameters.json",
  "support_boundary": "lowering ownership covers compile-latency provenance and emitted-code workload shape only when canonical lowering owners claim the behavior; timing rows, raw samples, report roots, output roots, generated rows, cache rows, comparative baselines, package staging, machine profiles, tool versions, and public command wrappers cannot create lowering support",
  "phase_support_claim_authority": false,
  "selectors": [
    "benchmark_classes:compile-latency",
    "compile_latency:measured_workloads",
    "compile_latency:output_root",
    "public_actions:compile_objc3c",
    "public_commands:compile_objc3c"
  ],
  "canonical_anchor": "tests/native/lowering",
  "retired_surface_rule": "fallback lowering paths, wrapper-only compile actions, compatibility-lowering performance claims, shim lowering rows, old-mode lowering rows, generated-only lowering rows, evidence-log lowering rows, migration-only lowering rows, timing-only lowering rows, cache-only lowering rows, local-only lowering rows, cross-machine lowering rows, and foreign-baseline lowering rows remain canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
