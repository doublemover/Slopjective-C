{
  "schema_version": 1,
  "catalog": "objc3c-compiler-throughput-behavior-owner-split",
  "owner_phase": "sema",
  "source_surface": "tests/tooling/fixtures/compiler_throughput/source_surface.json",
  "workload_manifest": "tests/tooling/fixtures/compiler_throughput/workload_manifest.json",
  "support_boundary": "semantic ownership covers typed diagnostics and source validation surfaced during compile workloads only when the row is not generated-only, evidence-log, shim, fallback, old-mode, migration-only, compatibility, cache-only, runner-only, docs-only, command-surface-only, package-only, or timing-only provenance; cache measurements cannot widen semantic support",
  "phase_support_claim_authority": false,
  "selectors": [
    "source_roots:native/objc3c/src/sema",
    "heavy_validation_actions:test-negative-expectations",
    "workloads:incremental-cache-invalidation",
    "source:tests/tooling/fixtures/native/macro_host_process_provider.objc3"
  ],
  "canonical_anchor": "tests/native/sema",
  "retired_surface_rule": "compatibility shim gates, unsupported feature claims, semantic recovery positives, semantic fallback gates, old-mode semantic rows, generated-only diagnostics, evidence-log diagnostics, migration-only semantic rows, cache-only semantic rows, runner-only semantic rows, and timing-only semantic rows are strict diagnostics, canonical rejection, absent-support, or provenance-only evidence"
}
