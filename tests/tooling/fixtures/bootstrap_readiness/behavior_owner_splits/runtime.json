{
  "schema_version": 1,
  "catalog": "objc3c-bootstrap-readiness-behavior-owner-split",
  "owner_phase": "runtime",
  "source_contracts": [
    "tests/tooling/fixtures/bootstrap_readiness"
  ],
  "support_boundary": "runtime ownership is non-positive for bootstrap readiness fixtures. Readiness state, intake recommendation, blocker counts, open issue counts, open milestone counts, and catalog task counts are tooling readiness state, not native runtime dispatch, storage, object-model, or ABI behavior support.",
  "phase_support_claim_authority": false,
  "selectors": [
    "tooling_fixture_family:bootstrap_readiness",
    "tooling_terms:readiness_state,intake_recommendation,blockers_open_count,catalog_open_task_count",
    "compiler_phase_boundary:no-runtime-positive-truth",
    "upstream_owner_required:tests/native/runtime"
  ],
  "canonical_anchor": "tests/native/runtime",
  "retired_surface_rule": "old-mode runtime bootstrap, compatibility runtime bootstrap, shim runtime bootstrap, fallback runtime bootstrap, generated-only runtime rows, evidence-log runtime rows, migration-only runtime rows, blocker-label-only runtime claims, and readiness-state rows promoted as runtime support are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
