{
  "schema_version": 1,
  "catalog": "objc3c-distribution-credibility-behavior-owner-split",
  "owner_phase": "runtime",
  "source_contracts": [
    "tests/tooling/fixtures/distribution_credibility/operator_release_policy.json",
    "tests/tooling/fixtures/distribution_credibility/release_drill_policy.json",
    "tests/tooling/fixtures/distribution_credibility/trust_signal_architecture.json"
  ],
  "support_boundary": "runtime ownership is non-positive for this distribution-credibility family except for provenance routing to canonical runtime owners. Install smoke, rollback, release drill, and end-to-end workflow labels are distribution credibility evidence labels here; runtime execution behavior remains owned by canonical runtime fixtures.",
  "phase_support_claim_authority": false,
  "selectors": [
    "trust_signal:package-channel-install-smoke",
    "required_drill_step:verify-install-smoke",
    "required_drill_step:verify-rollback-guidance",
    "incident_id:install-failure",
    "incident_id:rollback-failure",
    "compiler_phase_boundary:no-runtime-positive-truth-from-distribution-credibility-contracts",
    "upstream_owner_required:tests/native/runtime"
  ],
  "canonical_anchor": "tests/native/runtime",
  "retired_surface_rule": "old-mode runtime distribution rows, shim runtime distribution rows, fallback runtime distribution rows, compatibility runtime distribution rows, migration-only runtime rows, generated-only runtime rows, report-only runtime rows, install-smoke-label-only support, rollback-label-only support, and runner-only success are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
