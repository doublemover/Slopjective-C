{
  "schema_version": 1,
  "catalog": "objc3c-distribution-credibility-behavior-owner-split",
  "owner_phase": "lowering",
  "source_contracts": [
    "tests/tooling/fixtures/distribution_credibility"
  ],
  "support_boundary": "lowering ownership is non-positive for this distribution-credibility family. The family contains JSON distribution credibility policy fixtures only, with no checked-in lowering output truth.",
  "phase_support_claim_authority": false,
  "selectors": [
    "absent:tests/tooling/fixtures/distribution_credibility/**/*-lowering.expect.txt",
    "tooling_fixture_family:distribution_credibility",
    "compiler_phase_boundary:no-lowering-positive-truth-from-distribution-credibility-contracts",
    "upstream_owner_required:tests/native/lowering"
  ],
  "canonical_anchor": "tests/native/lowering",
  "retired_surface_rule": "old-mode lowering distribution rows, shim lowering distribution rows, fallback lowering distribution rows, compatibility lowering distribution rows, migration-only lowering rows, generated-only lowering rows, evidence-log lowering rows, and distribution-policy-only lowering support are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
