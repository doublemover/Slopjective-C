{
  "schema_version": 1,
  "catalog": "objc3c-security-hardening-behavior-owner-split",
  "owner_phase": "lowering",
  "source_contracts": [
    "tests/tooling/fixtures/security_hardening/macro_package_provenance_trust_policy.json",
    "tests/tooling/fixtures/security_hardening/source_surface.json"
  ],
  "support_boundary": "lowering ownership is non-positive for this security-hardening family. The macro policy says accepted macro provenance must be ready for lowering and runtime, but this fixture family does not contain checked-in lowering output truth; lowering support must route to canonical lowering fixtures.",
  "phase_support_claim_authority": false,
  "selectors": [
    "required_enforcement_flag:ready_for_lowering_and_runtime",
    "absent:tests/tooling/fixtures/security_hardening/**/*-lowering.expect.txt",
    "tooling_fixture_family:security_hardening",
    "compiler_phase_boundary:no-lowering-positive-truth-from-security-contracts",
    "upstream_owner_required:tests/native/lowering"
  ],
  "canonical_anchor": "tests/native/lowering",
  "retired_surface_rule": "old-mode lowering security rows, shim lowering security rows, fallback lowering security rows, compatibility lowering security rows, generated-only lowering rows, report-only lowering rows, migration-only lowering rows, readiness-label-only lowering claims, and security-contract-only lowering support are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
