{
  "schema_version": 1,
  "catalog": "objc3c-platform-hardening-behavior-owner-split",
  "owner_phase": "canonical_rejection",
  "source_contracts": [
    "tests/tooling/fixtures/platform_hardening/boundary_inventory.json",
    "tests/tooling/fixtures/platform_hardening/platform_support_tier_policy.json",
    "tests/tooling/fixtures/platform_hardening/unsupported_host_fail_closed_policy.json",
    "tests/tooling/fixtures/platform_hardening/toolchain_archive_claim_policy.json",
    "tests/tooling/fixtures/platform_hardening/toolchain_range_replay_contract.json"
  ],
  "support_boundary": "canonical rejection owns platform-hardening rows that look like support but are unsupported, overbroad, unproved, tool-probe-only, generated-only, evidence-log, old-mode, gate, retired route, compatibility, migration-only, runner-only, workflow-label-only, or directory-label-only. Gap claims and forbidden claims are rejection/absent-support evidence, not positive platform support.",
  "phase_support_claim_authority": false,
  "negative_claim_authority": true,
  "selectors": [
    "gap_claim:no non-windows-x64 support claim",
    "gap_claim:no cross-platform parity claim",
    "gap_claim:no system package manager publication claim",
    "gap_claim:no signed or notarized installer claim",
    "gap_claim:no multi-host packaged install matrix claim",
    "forbidden_claim:cross-platform parity",
    "forbidden_claim:supported on every LLVM host",
    "forbidden_claim:implicitly supported because the source tree parses",
    "forbidden_claim:package-manager parity across unproved hosts",
    "forbidden_claim:cross-host archive support",
    "forbidden_claim:cross-platform installer parity",
    "forbidden_claim:all LLVM major versions supported",
    "forbidden_claim:archive publication implies installer or update support on every host",
    "unsupported_claim:best effort supported",
    "unsupported_claim:probably compatible",
    "unsupported_claim:supported if LLVM is installed",
    "*old-mode*",
    "*old_mode*",
    "*gate*",
    "*retired-route*",
    "*compatibility*",
    "*compat*",
    "*migration-only*",
    "*migration_only*",
    "*evidence-log*",
    "*evidence_log*",
    "*generated-only*",
    "*generated_only*"
  ],
  "canonical_anchor": "tests/conformance/hard_cutover_retired_surface_fixture_contracts.json",
  "retired_surface_rule": "non-windows-x64 support, cross-platform parity, unproved package manager publication, signed/notarized installer trust, multi-host install matrix, cross-host archive support, all-LLVM-version support, tool-probe-only support, evidence-log support, old-mode platform rows, gate platform rows, retired route platform rows, compatibility platform rows, migration-only rows, generated-only rows, evidence-log rows, widened support positives, and alternate acceptance paths are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
