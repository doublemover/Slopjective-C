{
  "schema_version": 1,
  "catalog": "objc3c-release-foundation-behavior-owner-split",
  "owner_phase": "runtime",
  "source_contracts": [
    "tests/tooling/fixtures/release_foundation/artifact_taxonomy.json",
    "tests/tooling/fixtures/release_foundation/release_payload_policy.json",
    "tests/tooling/fixtures/release_foundation/distribution_audit.json"
  ],
  "support_boundary": "runtime ownership is non-positive for this release-foundation family except for provenance routing to canonical runtime owners. Runtime-library, native executable, execution smoke, and execution replay payload labels are selected release payload membership labels here; runtime execution behavior remains owned by canonical runtime fixtures.",
  "phase_support_claim_authority": false,
  "selectors": [
    "payload_family:runtime-library",
    "required_payload_binding:native_executable",
    "required_payload_binding:runtime_library",
    "required_payload_binding:execution_smoke_action",
    "required_payload_binding:execution_replay_action",
    "compiler_phase_boundary:no-runtime-positive-truth-from-release-foundation-contracts",
    "upstream_owner_required:tests/native/runtime"
  ],
  "canonical_anchor": "tests/native/runtime",
  "retired_surface_rule": "old-mode runtime release rows, gate runtime release rows, retired route runtime release rows, compatibility runtime release rows, migration-only runtime rows, generated-only runtime rows, evidence-log runtime rows, payload-label-only runtime claims, native-executable-label-only support, and runner-only success are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
