{
  "schema_version": 1,
  "catalog": "objc3c-release-foundation-behavior-owner-split",
  "owner_phase": "canonical_rejection",
  "source_contracts": [
    "tests/tooling/fixtures/release_foundation/source_surface.json",
    "tests/tooling/fixtures/release_foundation/distribution_trust_model.json",
    "tests/tooling/fixtures/release_foundation/distribution_audit.json",
    "tests/tooling/fixtures/release_foundation/reproducibility_policy.json",
    "tests/tooling/fixtures/release_foundation/release_payload_policy.json"
  ],
  "support_boundary": "canonical rejection owns release-foundation rows that look like support but are explicitly out of scope, unsupported, manual-only, installer-shaped, package-manager-shaped, signing-service-shaped, cross-host/cross-platform overclaims, network-hosted registry claims, generated-only, evidence-log, old-mode, shim, fallback, compatibility, migration-only, runner-only, workflow-label-only, or directory-label-only. Explicit non-goals, non-claims, rejected distribution claims, and manual-only input rows are rejection/absent-support evidence, not positive release capability truth.",
  "phase_support_claim_authority": false,
  "negative_claim_authority": true,
  "selectors": [
    "explicit_non_goal:no second installer-shaped payload",
    "explicit_non_goal:no hand-maintained digest manifest",
    "explicit_non_goal:no release claim that bypasses the runnable package manifest",
    "non_claim:no cross-host binary identity guarantee",
    "non_claim:no notarization or certificate-service guarantee",
    "non_claim:no package-manager ecosystem parity guarantee",
    "distribution_claim_rejected:system installer",
    "distribution_claim_rejected:package-manager publication",
    "distribution_claim_rejected:notarization or signing-service trust",
    "distribution_claim_rejected:cross-platform reproducibility guarantee",
    "distribution_claim_rejected:network-hosted release registry guarantee",
    "manual_only_input_rejected:spreadsheet-maintained digest lists",
    "manual_only_input_rejected:hand-written attestation blobs",
    "manual_only_input_rejected:manually curated payload inclusion lists outside checked-in contracts",
    "audited_channel:system-installer:status=deferred",
    "*old-mode*",
    "*old_mode*",
    "*shim*",
    "*fallback*",
    "*compatibility*",
    "*compat*",
    "*migration-only*",
    "*migration_only*",
    "*evidence-log*",
    "*evidence_log*",
    "*generated-only*",
    "*generated_only*"
  ],
  "canonical_anchor": "tests/conformance/hard_cutover_retired_surface_fixture_contracts.json",
  "retired_surface_rule": "second installer-shaped payloads, hand-maintained digest manifests, release bypassing runnable package manifest, system installer claims, package-manager publication, signing/notarization service trust, cross-host binary identity, cross-platform reproducibility, network-hosted release registry, manual digest lists, hand-written attestations, manual payload lists, old-mode release rows, shim release rows, fallback release rows, compatibility release rows, migration-only rows, generated-only rows, evidence-log rows, unsupported release positives, and alternate acceptance paths are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
