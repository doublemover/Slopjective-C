{
  "schema_version": 1,
  "catalog": "objc3c-release-foundation-behavior-owner-split",
  "owner_phase": "lowering",
  "source_contracts": [
    "tests/tooling/fixtures/release_foundation"
  ],
  "support_boundary": "lowering ownership is non-positive for this release-foundation family. The family contains JSON release policy fixtures only, with no checked-in lowering output truth.",
  "phase_support_claim_authority": false,
  "selectors": [
    "absent:tests/tooling/fixtures/release_foundation/**/*-lowering.expect.txt",
    "tooling_fixture_family:release_foundation",
    "compiler_phase_boundary:no-lowering-positive-truth-from-release-foundation-contracts",
    "upstream_owner_required:tests/native/lowering"
  ],
  "canonical_anchor": "tests/native/lowering",
  "retired_surface_rule": "old-mode lowering release rows, shim lowering release rows, fallback lowering release rows, compatibility lowering release rows, migration-only lowering rows, generated-only lowering rows, report-only lowering rows, and release-policy-only lowering support are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
