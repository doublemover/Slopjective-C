{
  "schema_version": 1,
  "catalog": "objc3c-activation-triggers-behavior-owner-split",
  "owner_phase": "lowering",
  "source_contracts": [
    "tests/tooling/fixtures/activation_triggers"
  ],
  "support_boundary": "lowering ownership is non-positive for activation trigger fixtures. This family does not contain lowering source fixtures or lowering expectations; accepted activation scenarios prove tooling gate behavior only, not compiler lowering behavior.",
  "phase_support_claim_authority": false,
  "selectors": [
    "absent:tests/tooling/fixtures/activation_triggers/**/*-ir.expect.txt",
    "absent:tests/tooling/fixtures/activation_triggers/**/*.dispatch-ir.expect.txt",
    "tooling_fixture_family:activation_triggers",
    "compiler_phase_boundary:no-lowering-positive-truth",
    "upstream_owner_required:tests/native/lowering"
  ],
  "canonical_anchor": "tests/native/lowering",
  "retired_surface_rule": "old-mode lowering activation, shim lowering activation, fallback lowering activation, compatibility lowering activation, generated-only lowering rows, report-only lowering rows, migration-only lowering rows, and activation expected-output rows promoted as lowering support are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
