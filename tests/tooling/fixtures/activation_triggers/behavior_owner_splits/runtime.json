{
  "schema_version": 1,
  "catalog": "objc3c-activation-triggers-behavior-owner-split",
  "owner_phase": "runtime",
  "source_contracts": [
    "tests/tooling/fixtures/activation_triggers"
  ],
  "support_boundary": "runtime ownership is non-positive for activation trigger fixtures. Queue state, trigger firing, release-scale, dispatch-evidence, and blocker labels are tooling activation states, not native runtime dispatch, storage, object-model, or ABI behavior support.",
  "phase_support_claim_authority": false,
  "selectors": [
    "tooling_fixture_family:activation_triggers",
    "tooling_terms:dispatch-open,dispatch_evidence_w*,release_scale_foundation_w*",
    "compiler_phase_boundary:no-runtime-positive-truth",
    "upstream_owner_required:tests/native/runtime"
  ],
  "canonical_anchor": "tests/native/runtime",
  "retired_surface_rule": "old-mode runtime activation, compatibility runtime activation, gate runtime activation, retired route runtime activation, generated-only runtime rows, evidence-log runtime rows, migration-only runtime rows, dispatch-label-only runtime claims, and activation queue-state rows promoted as runtime support are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
