{
  "schema_version": 1,
  "catalog": "objc3c-packaging-channels-behavior-owner-split",
  "owner_phase": "e2e",
  "source_contracts": [
    "tests/tooling/fixtures/packaging_channels/channel_architecture.json",
    "tests/tooling/fixtures/packaging_channels/installer_policy.json",
    "tests/tooling/fixtures/packaging_channels/metadata_surface.json",
    "tests/tooling/fixtures/packaging_channels/schema_surface.json",
    "tests/tooling/fixtures/packaging_channels/source_surface.json",
    "tests/tooling/fixtures/packaging_channels/supported_platforms.json",
    "tests/tooling/fixtures/packaging_channels/workflow_surface.json"
  ],
  "support_boundary": "e2e/tooling ownership covers only canonical strict packaging-channel behavior: release-foundation-derived channel manifests, package-runnable-toolchain payload derivation, portable-archive/local-installer/offline-bundle channel ids, windows-x64 channel publication, package channel and platform support matrix action routing, machine-generated install receipts, deterministic rollback, offline bootstrap without network, payload digest parity with the runnable package, required package manifest and receipt fields, and fail-closed package-channel workflow ordering. Platform support and toolchain archive capability claims remain routed to their platform-hardening owners. Generated artifacts and reports are evidence labels, not source truth.",
  "phase_support_claim_authority": true,
  "selectors": [
    "packaging_model:release-foundation-derived-channel-set",
    "canonical_payload_action:package-runnable-toolchain",
    "derivation_rule:all channel payloads derive from the runnable toolchain package manifest and release-foundation evidence artifacts",
    "channel_id:portable-archive",
    "channel_id:local-installer",
    "channel_id:offline-bundle",
    "default_platform_id:windows-x64",
    "platform_id:windows-x64",
    "host_os:windows",
    "host_arch:x64",
    "install_scope:local-root-only",
    "requires_machine_generated_receipt:true",
    "requires_deterministic_rollback:true",
    "requires_offline_bootstrap_without_network:true",
    "requires_payload_digest_parity_with_runnable_package:true",
    "public_action:check-packaging-channels-surface",
    "public_action:check-packaging-channels-schema-surface",
    "public_action:build-package-channels",
    "public_action:build-platform-support-matrix",
    "public_action:validate-packaging-channels",
    "public_action:validate-packaging-channels-end-to-end",
    "public_action:validate-platform-hardening",
    "public_action:validate-platform-hardening-end-to-end",
    "upstream_validate_action:validate-release-foundation",
    "workflow_child_actions:validate-release-foundation->check-packaging-channels-surface->check-packaging-channels-schema-surface->build-package-channels->build-platform-support-matrix",
    "supported_host_claim_owner:platform-hardening-support-source",
    "toolchain_archive_claim_owner:platform-hardening-build-package-validation",
    "toolchain_archive_claim_requires_owner:true",
    "package_payload_owner_action:package-runnable-toolchain",
    "required_manifest_field:contract_id",
    "required_manifest_field:generated_at_utc",
    "required_manifest_field:platform_id",
    "required_manifest_field:package_root",
    "required_manifest_field:platform_support_matrix",
    "required_manifest_field:supported_platform_ids",
    "required_manifest_field:support_tiers",
    "required_manifest_field:implemented_channels",
    "required_manifest_field:portable_archive",
    "required_manifest_field:installer_archive",
    "required_manifest_field:offline_archive",
    "required_receipt_field:contract_id",
    "required_receipt_field:install_root",
    "required_receipt_field:install_home",
    "required_receipt_field:bootstrap_script",
    "required_receipt_field:installed_at_utc",
    "package_bridge:objc3c"
  ],
  "canonical_anchor": "tests/tooling/fixtures/packaging_channels",
  "retired_surface_rule": "package-manager publication, auto-update service, signing/notarization claim, unsupported host success, second compiler payload, hand-curated install tree, installer-only metadata that bypasses the release manifest, system-wide install, network bootstrap, cross-platform installer parity, unsupported installer/package manager/signing/cross-host/network/manual support, generated-evidence-log success, runner-only success, workflow-label-only success, wrapper-only channel checks, old-mode packaging rows, shim packaging rows, fallback packaging rows, compatibility packaging rows, migration-only packaging rows, and directory-label-only e2e claims are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
