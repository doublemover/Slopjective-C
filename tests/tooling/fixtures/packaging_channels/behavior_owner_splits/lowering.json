{
  "schema_version": 1,
  "catalog": "objc3c-packaging-channels-behavior-owner-split",
  "owner_phase": "lowering",
  "source_contracts": [
    "tests/tooling/fixtures/packaging_channels"
  ],
  "support_boundary": "lowering ownership is non-positive for this packaging-channels family. The family contains JSON packaging policy fixtures only, with no checked-in lowering output truth.",
  "phase_support_claim_authority": false,
  "selectors": [
    "absent:tests/tooling/fixtures/packaging_channels/**/*-lowering.expect.txt",
    "tooling_fixture_family:packaging_channels",
    "compiler_phase_boundary:no-lowering-positive-truth-from-packaging-channels-contracts",
    "upstream_owner_required:tests/native/lowering"
  ],
  "canonical_anchor": "tests/native/lowering",
  "retired_surface_rule": "old-mode lowering packaging rows, shim lowering packaging rows, fallback lowering packaging rows, compatibility lowering packaging rows, migration-only lowering rows, generated-only lowering rows, report-only lowering rows, and packaging-policy-only lowering support are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
