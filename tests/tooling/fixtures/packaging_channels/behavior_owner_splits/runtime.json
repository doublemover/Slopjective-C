{
  "schema_version": 1,
  "catalog": "objc3c-packaging-channels-behavior-owner-split",
  "owner_phase": "runtime",
  "source_contracts": [
    "tests/tooling/fixtures/packaging_channels/installer_policy.json",
    "tests/tooling/fixtures/packaging_channels/supported_platforms.json"
  ],
  "support_boundary": "runtime ownership is non-positive for this packaging-channels family except for provenance routing to canonical runtime owners. Installer runtime requirements and required host tools are packaging execution environment labels here; runtime execution behavior remains owned by canonical runtime fixtures.",
  "phase_support_claim_authority": false,
  "selectors": [
    "installer_runtime_requirement:pwsh",
    "installer_runtime_requirement:filesystem access",
    "installer_runtime_requirement:machine-generated install receipt",
    "required_tool:pwsh",
    "required_tool:python",
    "required_tool:clang++",
    "compiler_phase_boundary:no-runtime-positive-truth-from-packaging-channels-contracts",
    "upstream_owner_required:tests/native/runtime"
  ],
  "canonical_anchor": "tests/native/runtime",
  "retired_surface_rule": "old-mode runtime packaging rows, shim runtime packaging rows, fallback runtime packaging rows, compatibility runtime packaging rows, migration-only runtime rows, generated-only runtime rows, report-only runtime rows, installer-runtime-label-only support, host-tool-label-only support, and runner-only success are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
