{
  "schema_version": 1,
  "catalog": "objc3c-package-ecosystem-behavior-owner-split",
  "owner_phase": "parser",
  "source_contracts": [
    "tests/tooling/fixtures/package_ecosystem"
  ],
  "support_boundary": "parser ownership is non-positive for this package-ecosystem family. Workspace ids, package ids, lockfile fields, registry layers, mirror labels, schema names, public action labels, package bridge labels, generated artifact paths, and report paths do not define Objective-C 3 parser support.",
  "phase_support_claim_authority": false,
  "selectors": [
    "tooling_fixture_family:package_ecosystem",
    "workspace_id:stdlib-workspace",
    "workspace_id:canonical-application-workspace",
    "workspace_id:runnable-package-workspace",
    "lock_required_field:packages",
    "lock_required_field:dependencies",
    "compiler_phase_boundary:no-parser-positive-truth-from-package-ecosystem-contracts",
    "upstream_owner_required:tests/native/parser"
  ],
  "canonical_anchor": "tests/native/parser",
  "retired_surface_rule": "old-mode parser package rows, shim parser package rows, fallback parser package rows, compatibility parser package rows, migration-only parser rows, generated-only parser rows, report-only parser rows, JSON-contract-only parser claims, and package-label-only parser claims are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
