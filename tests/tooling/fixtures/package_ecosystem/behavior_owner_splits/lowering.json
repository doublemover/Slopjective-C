{
  "schema_version": 1,
  "catalog": "objc3c-package-ecosystem-behavior-owner-split",
  "owner_phase": "lowering",
  "source_contracts": [
    "tests/tooling/fixtures/package_ecosystem"
  ],
  "support_boundary": "lowering ownership is non-positive for this package-ecosystem family. The family contains JSON package ecosystem policy fixtures only, with no checked-in lowering output truth.",
  "phase_support_claim_authority": false,
  "selectors": [
    "absent:tests/tooling/fixtures/package_ecosystem/**/*-lowering.expect.txt",
    "tooling_fixture_family:package_ecosystem",
    "compiler_phase_boundary:no-lowering-positive-truth-from-package-ecosystem-contracts",
    "upstream_owner_required:tests/native/lowering"
  ],
  "canonical_anchor": "tests/native/lowering",
  "retired_surface_rule": "old-mode lowering package rows, shim lowering package rows, fallback lowering package rows, compatibility lowering package rows, migration-only lowering rows, generated-only lowering rows, report-only lowering rows, and package-policy-only lowering support are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
