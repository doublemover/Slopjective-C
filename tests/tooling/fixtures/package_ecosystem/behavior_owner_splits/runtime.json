{
  "schema_version": 1,
  "catalog": "objc3c-package-ecosystem-behavior-owner-split",
  "owner_phase": "runtime",
  "source_contracts": [
    "tests/tooling/fixtures/package_ecosystem/boundary_inventory.json",
    "tests/tooling/fixtures/package_ecosystem/local_workspace_mirror_semantics.json",
    "tests/tooling/fixtures/package_ecosystem/package_authoring_workflow_contract.json"
  ],
  "support_boundary": "runtime ownership is non-positive for this package-ecosystem family except for provenance routing to canonical runtime owners. Runnable stdlib/application workflow labels and package replay labels are package ecosystem validation labels here; runtime execution behavior remains owned by canonical runtime fixtures.",
  "phase_support_claim_authority": false,
  "selectors": [
    "required_action:validate-runnable-package-ecosystem",
    "required_action:validate-runnable-stdlib-program",
    "required_action:validate-runnable-application-architecture",
    "replay_model:objc3c-package-bridge-action-list",
    "compiler_phase_boundary:no-runtime-positive-truth-from-package-ecosystem-contracts",
    "upstream_owner_required:tests/native/runtime"
  ],
  "canonical_anchor": "tests/native/runtime",
  "retired_surface_rule": "old-mode runtime package rows, gate runtime package rows, retired route runtime package rows, compatibility runtime package rows, migration-only runtime rows, generated-only runtime rows, evidence-log runtime rows, package-replay-label-only support, runnable-workflow-label-only support, and runner-only success are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
