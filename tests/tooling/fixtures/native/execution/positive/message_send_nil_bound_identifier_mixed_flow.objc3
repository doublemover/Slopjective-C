// Positive execution fixture: mixed immutable/mutable nil-bound receiver optional sends remain deterministic.
module MessageSendNilBoundIdentifierMixedFlowExecution;

fn main() {
  let stable = nil;
  let mutable = nil;
  mutable = nil;
  let stable_sent = [stable? ping];
  let mutable_sent = [mutable? pong];
  return stable_sent + mutable_sent + 14;
}
