module ByrefCaptureDuplicateBindingExec;

fn main() -> i32 {
  let counter = 1;
  let closure = ^[__block counter, byref counter] (i32 add) {
    counter = counter + add;
    return counter;
  };
  return closure(1);
}
