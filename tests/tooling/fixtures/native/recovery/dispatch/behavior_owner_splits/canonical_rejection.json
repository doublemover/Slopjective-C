{
  "schema_version": 1,
  "catalog": "objc3c-native-recovery-dispatch-behavior-owner-split",
  "owner_phase": "canonical_rejection",
  "source_contracts": [
    "tests/tooling/fixtures/native/recovery/dispatch"
  ],
  "support_boundary": "canonical rejection owns any recovery dispatch row that looks like support but is old-mode, gate, retired route, compatibility, generated-only, evidence-log, migration-only, unsupported, alternate acceptance, runner-only, sidecar-only, directory-label-only, missing upstream phase ownership, or outside the hand-authored positive behavior boundary.",
  "phase_support_claim_authority": false,
  "selectors": [
    "*old-mode*",
    "*old_mode*",
    "*gate*",
    "*retired-route*",
    "*compatibility*",
    "*compat*",
    "*legacy*",
    "*generated-only*",
    "*generated_only*",
    "*evidence-log*",
    "*evidence_log*",
    "*migration-only*",
    "*migration_only*",
    "*unsupported*",
    "*alternate-acceptance*",
    "*alternate_acceptance*",
    "*runner-only*",
    "*runner_only*",
    "*sidecar-only*",
    "*sidecar_only*",
    "*directory-label-only*",
    "*directory_label_only*",
    "*missing-upstream*",
    "*missing_upstream*",
    "message_send_six_args.meta.json:native_compile_args-only"
  ],
  "canonical_anchor": "tests/conformance/hard_cutover_retired_surface_fixture_contracts.json",
  "retired_surface_rule": "old-mode, gate, retired route, compatibility, generated-only, evidence-log, migration-only, unsupported, alternate-acceptance, runner-only, sidecar-only, directory-label-only, and missing-upstream recovery dispatch claims are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
