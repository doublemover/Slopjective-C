module InstancetypeAnnotationAliasSignature;

@protocol InstancetypeAnnotationProtocol
@end

fn choose(flag: bool, when_true: instancetype, when_false: instancetype<InstancetypeAnnotationProtocol>) -> instancetype {
  if (flag) {
    return when_true;
  }
  return when_false;
}

fn main() -> i32 {
  return choose(true, 41, 9) + 1;
}
