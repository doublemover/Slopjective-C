// Positive fixture: mixed immutable/mutable nil-bound receiver flows use optional sends and stay on the canonical i32 dispatch operand path.
module MessageSendNilBoundIdentifierMixedFlow;

fn main() {
  let stable = nil;
  let mutable = nil;
  mutable = nil;
  let stable_sent = [stable? ping];
  let mutable_sent = [mutable? pong];
  return stable_sent + mutable_sent + 14;
}
