// Negative fixture: declaration qualifiers must precede `fn`.
// Expected diagnostic code(s): O3P100.
module NegativeMisplacedQualifierFnPure;

fn pure add(lhs: i32, rhs: i32) -> i32;

fn main() -> i32 {
  return 0;
}
