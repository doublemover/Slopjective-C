{
  "schema_version": 1,
  "catalog": "objc3c-native-recovery-negative-behavior-owner-split",
  "owner_phase": "canonical_rejection",
  "source_contracts": [
    "tests/tooling/fixtures/native/recovery/negative/*.objc3"
  ],
  "support_boundary": "canonical rejection owns recovery negative fixtures as fail-closed evidence: lexer/parser malformed source, semantic type and flow failures, unsupported syntax or types, conservative return/control-flow rejection, duplicate declarations, invalid aliases, protocol/generic/nullability conflicts, pure/extern violations, runtime-dispatch residues, and any row that looks like support but is generated-only, evidence-log, migration-only, old-mode, shim, fallback, compatibility, wrapper-only, runner-only, e2e-only, or directory-label-only.",
  "phase_support_claim_authority": false,
  "negative_claim_authority": true,
  "selectors": [
    "tests/tooling/fixtures/native/recovery/negative/*.objc3",
    "diagnostic_code:O3L*",
    "diagnostic_code:O3P*",
    "diagnostic_code:O3S*",
    "*old-mode*",
    "*old_mode*",
    "*shim*",
    "*fallback*",
    "*compatibility*",
    "*compat*",
    "*legacy*",
    "*generated-only*",
    "*generated_only*",
    "*evidence-log*",
    "*evidence_log*",
    "*migration-only*",
    "*migration_only*",
    "*wrapper-only*",
    "*wrapper_only*",
    "*runner-only*",
    "*runner_only*",
    "*e2e-only*",
    "*e2e_only*",
    "*directory-label-only*",
    "*directory_label_only*",
    "*unsupported*",
    "*alternate-acceptance*",
    "*alternate_acceptance*"
  ],
  "canonical_anchor": "tests/conformance/hard_cutover_retired_surface_fixture_contracts.json",
  "retired_surface_rule": "old-mode, shim, fallback, compatibility, generated-only, evidence-log, migration-only, wrapper-only, runner-only, e2e-only, directory-label-only, unsupported, and alternate-acceptance recovery negative claims are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
