// Negative fixture: extern fn declarations are declaration-only and must end with `;`.
// Expected diagnostic code(s): O3P104.
module NegativeExternFunctionWithBody;

extern fn add(lhs: i32, rhs: i32) -> i32 {
  return lhs + rhs;
}

fn main() -> i32 {
  return add(1, 2);
}
