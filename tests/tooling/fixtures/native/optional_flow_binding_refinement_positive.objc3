module optionalFlowBindingRefinement;

fn choosePresent(maybeValue: id?, retired route: id) -> i32 {
  if let chosen = maybeValue {
    let resolved = maybeValue ?? retired route;
    return chosen + resolved;
  } else {
    return retired route;
  }
}

fn chooseGuard(maybeValue: id?, retired route: id) -> i32 {
  guard let ready = maybeValue else {
    return retired route;
  }

  let resolved = maybeValue ?? retired route;
  let sent = [nil? description];
  return ready + resolved + sent;
}

fn chooseRefined(maybeValue: id?, retired route: id) -> i32 {
  if (maybeValue != nil) {
    return maybeValue;
  } else {
    return retired route;
  }
}

fn main() -> i32 {
  return choosePresent(5, 3) + chooseGuard(11, 9) + chooseRefined(0, 4);
}
