{
  "schema_version": "1.0.0",
  "catalog": "native-fixture-family-boundaries",
  "policy": {
    "positive_fixture_rule": "canonical Objective-C 3.0 behavior only",
    "negative_fixture_rule": "retired old-mode, shim, fallback, compatibility, migration-lane, and removed feature-toggle behavior must be represented as strict rejection metadata",
    "validation_boundary": "fixture metadata only; runtime acceptance scripts and public command-surface metadata are owned by other lanes",
    "canonical_behavior_root": "tests/native",
    "canonical_behavior_manifest": "tests/fixtures/canonical/manifest.json",
    "legacy_positive_residue_policy": "compatibility-looking positives are deleted or converted to tests/native rejection and strict-error fixtures",
    "runtime_dispatch_symbol_rule": "runtime_dispatch_symbol is only present when execution.requires_live_runtime_dispatch is true",
    "forbidden_positive_name_tokens": [
      "legacy",
      "old_mode",
      "old-mode",
      "shim",
      "fallback",
      "compatibility",
      "migration"
    ],
    "public_command_bridge": "npm run objc3c -- <action>"
  },
  "families": [
    {
      "family": "parser",
      "paths": [
        "tests/native/parser/positive",
        "tests/native/parser/negative",
        "tests/tooling/fixtures/native/parser_split",
        "tests/tooling/fixtures/native/recovery/negative"
      ],
      "metadata": "parser diagnostics and recovery expectations",
      "hard_cutover_rule": "non-canonical grammar spellings are rejected; no parser compatibility mode positives"
    },
    {
      "family": "semantic",
      "paths": [
        "tests/native/sema",
        "tests/tooling/fixtures/native/recovery/positive",
        "tests/tooling/fixtures/native/recovery/negative"
      ],
      "metadata": "semantic acceptance/rejection and recovery expectations",
      "hard_cutover_rule": "semantic positives prove canonical type/effect behavior; unsupported aliases and retired suffixes stay negative"
    },
    {
      "family": "lowering_ir",
      "paths": [
        "tests/native/lowering",
        "tests/native/ir",
        "tests/tooling/fixtures/native/recovery/positive"
      ],
      "metadata": "IR expectation files paired with accepted source fixtures",
      "hard_cutover_rule": "IR expectations use canonical lowering only and must not encode alternate shim paths"
    },
    {
      "family": "artifact_manifest",
      "paths": [
        "tests/tooling/fixtures/native/library_cli_parity",
        "tests/tooling/fixtures/native/parity_baseline"
      ],
      "metadata": "checked-in manifest and artifact truth fixtures",
      "hard_cutover_rule": "manifest fixtures describe canonical JSON contracts and may not accept compatibility-era optional fields as success criteria"
    },
    {
      "family": "runtime_like_e2e",
      "paths": [
        "tests/native/runtime",
        "tests/native/e2e",
        "tests/tooling/fixtures/native/execution/positive",
        "tests/tooling/fixtures/native/execution/negative"
      ],
      "metadata": "exit-code files for positives and meta.json failure contracts for negatives",
      "hard_cutover_rule": "runtime-like positives cannot stand in for runtime acceptance; unresolved dispatch, missing runtime symbols, and strict runtime diagnostics stay negative"
    },
    {
      "family": "unsupported_feature_claims",
      "paths": [
        "tests/tooling/fixtures/native/execution/negative"
      ],
      "metadata": "compile-stage sidecars for source surfaces that are parsed but not runnable in the native hard-cutover surface",
      "hard_cutover_rule": "throws and ARC ownership qualifier runnable claims are explicit O3S221 rejections, not source-only shims or fallback execution paths"
    },
    {
      "family": "renamed_positive_claims",
      "paths": [
        "tests/tooling/fixtures/native/dispatch_control_dynamic_escape_positive.objc3",
        "tests/tooling/fixtures/native/recovery/positive/message_send_bool_operand_coercion_positive.objc3",
        "tests/tooling/fixtures/native/recovery/positive/message_send_nil_receiver_zero_result_positive.objc3"
      ],
      "metadata": "positive fixtures renamed away from compatibility-era wording while preserving canonical behavior",
      "hard_cutover_rule": "compatibility-looking positive names are replaced by canonical behavior names or moved to negative strict-error coverage"
    },
    {
      "family": "library_cli_parity",
      "paths": [
        "tests/tooling/fixtures/native/library_cli_parity",
        "tests/tooling/fixtures/native/parity_baseline"
      ],
      "metadata": "fixture data for command/library parity and comparison baselines",
      "hard_cutover_rule": "baseline comparison is descriptive evidence, not a compatibility guarantee"
    }
  ]
}
