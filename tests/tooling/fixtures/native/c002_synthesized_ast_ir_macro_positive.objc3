module MetaprogrammingSynthesizedArtifactsMacro;

pure fn traced() -> i32 __attribute__((objc_macro(named("Trace")), objc_macro_package(named("std.metaprogramming.trace")), objc_macro_provenance(named("sha256:abc123")))) {
  return 7;
}

@interface Widget __attribute__((objc_derive(named("Equality"))))
@property (strong, behavior=Observed) id value;
@end

@implementation Widget
@property (strong, behavior=Observed) id value;
@end
