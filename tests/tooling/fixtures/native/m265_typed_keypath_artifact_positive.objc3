module typedKeyPathArtifactPositive;

@protocol Named
@property (readonly) id title;
@end

@interface Person
@property (readonly) id<Named>*? name;
@end

@implementation Person
@end

fn main() -> i32 {
  let path = @keypath(Person, name);
  if (path != nil) {
    return 7;
  }
  return 0;
}
