{
  "schema_version": 1,
  "catalog": "objc3c-frontend-c-api-behavior-owner-split",
  "owner_phase": "ir",
  "source_contracts": [
    "tests/tooling/fixtures/native/frontend_c_api_runner_contract.json",
    "tests/tooling/fixtures/native/frontend_c_api_helper_contract.json"
  ],
  "support_boundary": "IR ownership covers manifest, IR, object, runtime metadata binary, diagnostics, and artifact accessor path contracts as provenance; artifact path presence cannot establish behavior support",
  "phase_support_claim_authority": false,
  "selectors": [
    "required_path_fields:manifest",
    "required_path_fields:ir",
    "required_path_fields:object",
    "required_path_fields:runtime_metadata_binary",
    "required_path_fields:diagnostics",
    "required_helpers:objc3c_frontend_c_result_artifact_path",
    "required_helpers:objc3c_frontend_c_result_artifact_path_view",
    "required_helpers:objc3c_frontend_c_result_has_artifact"
  ],
  "canonical_anchor": "tests/native/ir",
  "retired_surface_rule": "generated-only rows, generated-artifact-only support, evidence-log artifact claims, shim/fallback artifact paths, old-mode IR artifact rows, migration-only artifact claims, compatibility artifact rows, missing artifact paths, empty artifact paths, undefined artifact kinds, and artifact accessor mismatch are provenance-only, canonical rejection, or strict-error evidence"
}
