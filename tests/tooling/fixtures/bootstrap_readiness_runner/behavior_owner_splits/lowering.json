{
  "schema_version": 1,
  "catalog": "objc3c-bootstrap-readiness-runner-behavior-owner-split",
  "owner_phase": "lowering",
  "source_contracts": [
    "tests/tooling/fixtures/bootstrap_readiness_runner"
  ],
  "support_boundary": "lowering ownership is non-positive for bootstrap-readiness-runner fixtures. The family contains JSON runner scenario snapshots only, with no checked-in lowering output truth.",
  "phase_support_claim_authority": false,
  "selectors": [
    "absent:tests/tooling/fixtures/bootstrap_readiness_runner/**/*-lowering.expect.txt",
    "tooling_fixture_family:bootstrap_readiness_runner",
    "compiler_phase_boundary:no-lowering-positive-truth-from-bootstrap-readiness-runner",
    "upstream_owner_required:tests/native/lowering"
  ],
  "canonical_anchor": "tests/native/lowering",
  "retired_surface_rule": "old-mode lowering readiness-runner rows, shim lowering rows, fallback lowering rows, compatibility lowering rows, migration-only lowering rows, generated-only lowering rows, report-only lowering rows, and runner-state lowering support are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
