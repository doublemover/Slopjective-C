{
  "schema_version": 1,
  "catalog": "objc3c-performance-governance-behavior-owner-split",
  "owner_phase": "lowering",
  "source_contracts": [
    "tests/tooling/fixtures/performance_governance"
  ],
  "support_boundary": "lowering ownership is non-positive for this performance-governance family. The family contains JSON policy fixtures for performance budgets, reports, claims, waivers, lab profiles, schemas, and workflows, with no checked-in lowering output truth.",
  "phase_support_claim_authority": false,
  "selectors": [
    "absent:tests/tooling/fixtures/performance_governance/**/*-lowering.expect.txt",
    "tooling_fixture_family:performance_governance",
    "compiler_phase_boundary:no-lowering-positive-truth-from-performance-governance-contracts",
    "upstream_owner_required:tests/native/lowering"
  ],
  "canonical_anchor": "tests/native/lowering",
  "retired_surface_rule": "old-mode lowering performance-governance rows, shim lowering rows, fallback lowering rows, compatibility lowering rows, migration-only lowering rows, generated-only lowering rows, evidence-log lowering rows, and governance-policy-only lowering support are canonical rejection, strict-error, absent-support, or provenance-only evidence"
}
