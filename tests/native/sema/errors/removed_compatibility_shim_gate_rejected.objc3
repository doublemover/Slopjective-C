// Canonical sema rejection fixture: compatibility shim gates never authorize semantic positives.
module RemovedCompatibilityShimGateRejected;

@interface ShimmedBox : NSObject
- (id)value;
@end

fn main() -> i32 {
  return 0;
}
