# M315-E001 Expectations

Contract ID: `objc3c.cleanup.source-hygiene.proof-hygiene.gate/m315-e001-v1`

## Outcome
- freeze the lane-E gate over the live `M315` source-hygiene and proof-hygiene surfaces;
- require the D001 historical contract, the D002 live anti-noise guard, and the B005 native-source sweep to agree on the final post-cleanup state;
- prevent milestone closeout from claiming more than the remaining quarantined residual classes and truthful synthetic-fixture labeling support.

## Required truths
- `M315-D001` continues to record the historical D002 zero-target classes while allowing the current live count for those classes to be zero;
- `M315-D002` remains green and publishes:
  - zero target-file zero-target hits;
  - `57` post-cleanup milestone-token lines in the B005 sweep;
  - no tracked `compiler/objc3c/*.py` sources;
- the stable identifier family remains `advanced_integration_closeout_signoff`;
- the remaining residual classes are exactly:
  - `legacy_fixture_path_reference`
  - `dependency_issue_array`
  - `next_issue_schema_field`
  - `issue_key_schema_field`
- the D002 workflow surface exists and remains wired to the live checker.

## Exit evidence
- issue-local gate summary at `tmp/reports/m315/M315-E001/source_hygiene_proof_hygiene_gate_summary.json`;
- machine-readable gate contract in `spec/planning/compiler/m315/`.
