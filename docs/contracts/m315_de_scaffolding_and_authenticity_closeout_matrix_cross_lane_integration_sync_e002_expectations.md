# M315-E002 Expectations

Contract ID: `objc3c.cleanup.de-scaffolding.authenticity.closeout.matrix/m315-e002-v1`

## Outcome
- publish the final cross-lane closeout matrix for `M315`;
- prove that the milestone exits with only the explicitly quarantined residue classes remaining;
- preserve the distinction between historical planning residue, live product-code cleanup, and fenced synthetic parity fixtures.

## Required truths
- every predecessor `M315-A001` through `M315-E001` checker still passes;
- the final native-source sweep remains at `57` milestone-token lines with only the four quarantined residual classes;
- the D001 historical contract still records the old zero-target counts while the live current count for those targets remains zero;
- the D002 live anti-noise guard remains green and the workflow surface remains present;
- the milestone may claim source-hygiene and proof-hygiene cleanup only to that exact boundary, not beyond it.

## Exit evidence
- closeout summary at `tmp/reports/m315/M315-E002/de_scaffolding_authenticity_closeout_matrix_summary.json`;
- machine-readable closeout contract in `spec/planning/compiler/m315/`.