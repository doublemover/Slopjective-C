# M270 Actor Lowering And Metadata Contract And Architecture Freeze Expectations (C001)

- Contract ID: `objc3c-part7-actor-lowering-and-metadata-contract/m270-c001-v1`
- Surface path: `frontend.pipeline.semantic_surface.objc_part7_actor_lowering_and_metadata_contract`
- The lowering handoff must freeze deterministic actor metadata record planning, actor isolation-thunk planning, and actor hop-artifact planning.
- The contract must be derived from the live actor source, enforcement, and hazard packets instead of inventing a parallel actor model.
- Live thunk bodies and runnable actor runtime entrypoints remain deferred to later `M270` lane-C work.
