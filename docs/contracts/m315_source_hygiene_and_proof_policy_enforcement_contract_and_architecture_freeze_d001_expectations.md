# M315-D001 Expectations

Contract ID: `objc3c.cleanup.source-hygiene-and-proof-policy.enforcement/m315-d001-v1`

## Outcome
- freeze the live enforcement boundary for the remaining `M315` source-hygiene and proof-policy work;
- separate the residue classes that `M315-D002` must eliminate from the older quarantined schema/path residue that is not part of the live anti-noise guard;
- freeze the synthetic parity-fixture authenticity surface and the retired prototype-compiler ban as first-class enforcement targets.

## Required truths
- `M315-D002` must drive the targeted product-code milestone residue classes to zero:
  - `transitional_source_model`
  - `legacy_m248_surface_identifier`
- the live anti-noise guard must fail closed on:
  - tracked `compiler/objc3c/*.py` reintroduction
  - unlabeled or partially labeled `library_cli_parity` synthetic artifacts
  - new milestone-coded residue in tracked product-code roots after the D002 replacement sweep
- quarantined historical residue outside the D002 cleanup target remains explicitly recorded instead of silently absorbed into the live guard.

## Scope
- tracked product-code roots under `native/objc3c/src`
- synthetic parity fixture root `tests/tooling/fixtures/native/library_cli_parity`
- retired prototype compiler archive boundary under `spec/governance/retired_surfaces/compiler_objc3c`

## Exit evidence
- issue-local checker summary at `tmp/reports/m315/M315-D001/source_hygiene_proof_policy_enforcement_contract_summary.json`
- machine-readable enforcement contract in `spec/planning/compiler/m315/`
