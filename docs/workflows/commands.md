# Commands

Command support is generated from checked-in workflow contracts and public
command docs. Capability claims route through `docs/support/capability_matrix.md`.
User-facing workflow docs advertise the `npm run objc3c -- <action>` bridge only.
Retired aliases and implementation-helper commands are not public command surface.
